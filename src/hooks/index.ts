// Barrel exports for hooks
// Provides clean import interface for all custom hooks

export { useActiveSection } from './useActiveSection';
export { useMobileNavigation } from './useMobileNavigation';
export { useNavbarScroll } from './useNavbarScroll';
export { useSectionAnimation, useStaggeredAnimation } from './useSectionAnimation';

// Note: Type exports removed due to interface/type visibility issues
// Import types directly from individual hook files when needed
